{
  "name": "trenchfall",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "coop": "node server/co-op-server.mjs",
    "start": "node server/co-op-server.mjs",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node --check src/main.js && node scripts/check-order.mjs && node scripts/boot-smoke.mjs",
    "ship": "npm run check && node scripts/stamp.mjs"
  },
  "dependencies": {
    "three": "0.160.0",
    "ws": "^8.17.1"
  },
  "devDependencies": {
    "acorn": "^8.16.0",
    "acorn-walk": "^8.3.5",
    "vite": "^7.1.0"
  }
}
